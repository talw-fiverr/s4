import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class ManageGroups extends Component {
  render() {
    return (
      <div>this is the manage groups page</div>
    );
  }
}

ReactDOM.render(<ManageGroups />, document.querySelector('.container'));
